/** Width / height ratio used by CropModal and all cropped media previews */
export const CROP_ASPECT_RATIO = 1

/** Internal canvas resolution for scratch-card overlay (matches crop aspect) */
export const CROP_CANVAS_SIZE = 600
